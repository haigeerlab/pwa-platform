# ADR-0007：将契约与策略编译分离

## 状态

已接受

## 背景

类型和 schema 需要不依赖框架、浏览器环境的复用。策略归一化、安全基线合并、优先级和计划编译属于具有不同发布面的行为语义。

## 决策

`@pwa-platform/contracts` 负责可序列化结构、校验原语和诊断；`@pwa-platform/core` 负责编译为 `PwaPlan`。运行时和构建包消费编译后的计划，不得重新实现策略优先级。

## 影响

编译器实现可以演进，而公开契约保持稳定。编译器变更需要确定性计划测试和兼容性评审。
