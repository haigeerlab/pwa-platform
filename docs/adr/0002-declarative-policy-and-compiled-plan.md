# ADR-0002：暴露声明式策略并编译为可审计计划

## 状态

已接受

## 背景

原始 Service Worker 和 Workbox callback 会随框架变化，无法可靠审计其安全性或 SSR 行为。

## 决策

应用提供可序列化的 `PwaPolicy`；平台控制的身份和宿主产物将其编译为 `PwaPlan`。Workbox 保持为内部实现。

## 影响

平台可在发布前校验缓存拒绝规则和产物拓扑。特殊行为必须通过新增策略能力或批准的平台扩展实现，不能在 worker 中插入任意应用代码。
