# ADR-0005：默认采用提示更新并保留恢复 worker

## 状态

已接受

## 背景

立即激活 Service Worker 可能导致页面仍使用旧 chunk，而 worker 已提供新资源；这对直播会话、支付和表单填写尤其有害。

## 决策

默认使用提示更新。维护紧急清理 worker，它只移除平台拥有的缓存并停止 fetch 拦截。

## 影响

应用通过客户端 facade 拥有更新提示的展示。安全事故可在完成记录化运维审批后明确采用立即恢复路径。
