# ADR-0004：生产注册后的 PWA 身份不可变更

## 状态

已接受

## 背景

用户安装应用后变更 scope、Service Worker URL、manifest ID 或挂载路径，可能留下重叠注册和陈旧存储。

## 决策

身份由平台拥有并由 CI 校验。生产变更必须是具有名称的迁移，包含清理和回滚步骤，不能作为日常策略修改。

## 影响

首次生产注册需要审慎的身份评审。功能扩展应在不改变已安装应用身份的情况下保持向后兼容。
