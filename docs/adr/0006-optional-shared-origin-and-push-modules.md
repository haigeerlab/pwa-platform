# ADR-0006：同源治理与 Push 保持可选

## 状态

已接受

## 背景

大多数应用只使用一个 origin，不需要 scope 协调或 Push 投递。将任何一项设为强制运行时依赖都会为所有消费方增加复杂度和权限。

## 决策

在基础契约稳定后，将同源拓扑治理和 Push 实现为可选模块。

## 影响

普通应用保持轻量并聚焦框架接入。使用根 worker 加固定子 scope 的应用必须接受明确部署检查；Push 需要后端负责、订阅管理、偏好控制和去重。
