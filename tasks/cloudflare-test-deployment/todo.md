# Todo：cloudflare-test-deployment

- [x] T1 确定目标登记和命名；账户内名称已核对，实际 origin 在创建后登记
- [x] T2 完成隔离构建、上传白名单、身份／资产保留前置门禁和 dry-run；已有发布一律阻断，资产保留与再次发布在 T5 交付
- [x] 检查点 A：Cloudflare 返回 React/Vue 项目名与 origin，并用于独立构建身份
- [x] T3 首次部署并验证 React 独立 Pages 站（在线／离线冒烟；更新与恢复在 T5）
- [x] T4 首次部署并验证 Vue 独立 Pages 站（在线／离线冒烟；更新与恢复在 T5）
- [x] 检查点 B：两站桌面 Chrome N/N-1 与现场响应头检查；Chrome 153/152 的桌面场景及真实响应头已核验，React/Vue 主站真实安装确认和独立窗口 origin 已通过，Vue 已安装窗口断网重载通过；两站唯一显示名已部署，既有安装接受名称更新后的冷启动通过
- [ ] T5 完成更新、Pages 回滚和恢复 worker 演练，核验旧指纹资源；两站 drill 的更新／恢复／旧资源、两站 main 的 v2→v1→v2 原生回滚、R2 十部署索引及线上文件核验已通过；两站 main 后续部署均实测上传后自动索引／归档／保留审计，缺失对象与损坏副本反例、未知查询真实断网导航已通过。剩余：完整七日时间跨度、真正的第二台机器上的恢复（恢复脚本与同机隔离模拟已于 2026-09-22 完成，见 RC 修订；drill 后置步骤自动化已完成，见 DR 修订）
- [x] T6 完成 Nuxt Workers 本地可行性门禁：产物启动失败，未创建云端 Worker；SSR 现场能力保留未验证
- [x] T7 完成操作手册、文档基线、验证记录和 Spec Guard 只读核验；产物校验 0 失败，文档交付因 T5 剩余证据保持 `pending`（`attention`）

## 修订：线上发布事实采集与机器发布门禁

- [x] M1 规格修订与本计划（项目所有者已确认）
- [x] M2 构建时保存计划（ad24b1f；两站本地构建计划有效、身份一致、不在上传目录；单元 9 项、浏览器 47 项通过）
- [x] M3 发布包携带计划（7cf30f9；两站新发布包解包后计划与源一致；无计划的回执拒绝打包）
- [x] 检查点 A：两站本地构建记录含经校验的计划，发布包可取回计划（项目所有者 2026-09-22 确认）
- [x] M4 组装与结论（纯函数，TDD）（7ee40b5、43beb7f；首版验收发现同时间戳与非有限时间戳会使历史被截短后仍判完整，已修复并补测试；单元 42 项通过，Node 22/24 直接运行）
- [x] M5 采集、输出与入口（8c985bf、29eb93f；首版验收发现会跟随跨域重定向、经重定向观测响应头、请求无超时，已修复并补测试；单元与集成 87 项连续两次通过）
- [x] M6 带凭据一侧导出历史（0f169ba、48f04ba 及安全测试修正；首版验收发现普通审计路径被迫加载 TypeScript、导出路径在读取凭据之后才校验，已修复；安全测试改为以非空的无效凭据运行，回退时既不读钥匙串也不联网；真实导出尚未运行，需项目所有者同意）
- [x] 检查点 B：fixture 集成场景全部通过，导出格式与读取格式一致（项目所有者 2026-09-22 确认；`run.test.ts`"两次运行结果一致"在 M6 子代理处失败过一次，之后累计十余次未复现；2026-09-22 在上线前核验修订中查明根因为事实文件记录了夹具服务器的 Date 响应头，已修复）
- [x] M7 真实运行与收口（项目所有者同意后导出历史并核验两站 `main`：三项检查通过，6 个历史部署缺计划，保留检查未执行，结论未通过，符合预期；候选由 `--release=v2` 重建，与线上逐字节一致，未重新部署；见验证记录）

## 后续（M 系列完成后开始）

- [ ] 业务项目的发布门禁接入：转交 `vite-adapter`，先写规格（见[计划](plan.md)"后续事项"）。**2026-09-22 项目所有者决定延后**：业务项目的实际构建与部署方式尚未调研清楚，待了解后再启动

## 修订：上线前核验

- [x] P1 规格修订与本计划（项目所有者已确认）
- [x] P2 上线前的纯函数（TDD）（c4788a6；上线前与上线后共用同一历史校验函数，旧测试文件零改动；对抗性输入均判不完整；待决：空的生产历史目前判为不完整）
- [x] P3 `--pre-deploy` 模式的采集与入口（1cbe91c；上线后测试文件零改动；drill 槽位、非规范部署 ID、路径穿越 ID 均在联网前拒绝且不建输出；另修复"两次运行结果一致"的不稳定：根因是事实文件记录了夹具服务器每次响应的 Date，已改为除 date 外逐项比较，强制间隔 1.1 秒验证成立）
- [x] 检查点 A：fixture 上的上线前场景全部通过，上线后既有测试全部通过（145 项连续三轮通过；项目所有者 2026-09-22 确认）
- [x] P4 部署脚本的 `preview-candidate` 模式（54d6bdf、f57b025；与生产上传路径以 if…else 隔离，只以字面值 `--branch=candidate` 上传；安全测试在空临时根目录运行脚本副本；验收补充：缺计划在读凭据前拒绝、上传后出错时说明预览部署已创建；上传、新部署识别与 API 读回未经真实凭据验证，留待 P5）
- [x] 检查点 B：项目所有者同意两站各上传一次到 `candidate`（2026-09-22）
- [x] P5 真实运行与收口（不上传到 `main`）（两站预览部署 70a40c96…、e51571e7… 读回通过，唯一地址规律对预览成立；三项检查通过，7 个生产部署缺计划，保留检查未执行，结论未通过，符合预期；生产未受影响；见验证记录）

## 修订：`drill` 上传前预检与上传后自动步骤

- [x] DR1 规格修订与本计划（项目所有者已确认）
- [x] DR2 `drill` 预检与后置步骤（8dd8501；只新增、未删改 main 代码；后置步骤位于 deploy 分支内；两道拒绝均在读取凭据之前；161 项测试连续两次通过）
- [x] 检查点：项目所有者同意对一个站点的 `drill` 做一次真实部署（2026-09-22，先合入 main 再在主目录运行）
- [x] DR3 真实运行与收口（React drill 部署 ae41ddfd… 自动识别、写索引、归档；索引读回一致；见验证记录）

## 修订：从 R2 恢复运营状态

- [x] RC1 规格修订与本计划（项目所有者已确认）
- [x] RC2 恢复脚本与安全测试（c78c6ae；状态保护先于首个子进程；脚本不含部署、上传、写索引；171 项测试连续两次通过）
- [x] 检查点：项目所有者同意在临时克隆中做隔离模拟（只读；克隆检出 claude/machine-recovery 分支运行，2026-09-22）
- [x] RC3 隔离模拟与收口（两站在全新克隆中一条命令恢复成功，与主目录逐字节一致，新候选通过 deploy check；首次运行暴露规格第五步设计错误，已修正；同机隔离模拟，不等于独立机器）

## 修订：核验工具采集公开 HTML 响应头

- [x] HC1 规格修订与本计划（acfc5e5；项目所有者已确认）
- [x] HC2 纯函数层（路径推导、必需集、组装）（68f22ee、d388173；对照测试补上"离线页不在预缓存中"，三处变异均报红；分支上 5 个"全部通过"集成场景待 HC3 接上采集后恢复）
- [x] HC3 采集、事实文件与集成测试（e4f1b97；HTML 采集只跟随同源重定向，worker 经重定向仍记为未采集（既有测试）；两处变异均报红；192 项测试连续两次通过）
- [x] 检查点：项目所有者同意导出生产历史（只读 GET）并做真实运行（2026-09-22）
- [x] HC4 真实运行与收口（两站上线后与上线前四次核验：`html-headers` 均通过，三个 HTML 路径最终 `no-cache`、两个经 308；覆盖只缺 `release-retention`，结论 `pass: false` 符合预期；见验证记录）
